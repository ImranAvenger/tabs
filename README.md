# tabs
This project introduces basic DOM manipulation and event handling in JavaScript. The objective is to create a four-tab system using HTML/CSS. The first tab is active by default; clicking another hides the current content and displays the selected tab's content.
